spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      host: localhost
      port: 26017
      database: geometry
      connection-string: mongodb://localhost:26017/geometry
